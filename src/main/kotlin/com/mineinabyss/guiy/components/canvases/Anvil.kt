package com.mineinabyss.guiy.components.canvases

/*
@Composable
fun GuiyOwner.Anvil(
    viewers: List<Player>,
    title: String,
    onClose: (InventoryCloseScope.(player: Player) -> Unit)? = null,
    onResultClicked: ClickScope.(String) -> Unit = {},
    modifier: Modifier = Modifier,
//    input: String,
//    onValueChange: (String) -> Unit,
    children: @Composable () -> Unit,
) = Inventory(
    measurePolicy = StaticMeasurementPolicy,
    type = InventoryType.ANVIL,
    viewers = viewers,
    title = title,
    onClose = onClose,
    modifier = modifier.size(3, 1).clickable {
        if (slot == 2) {
            val itemTitle = item?.i18NDisplayName ?: return@clickable
            onResultClicked(itemTitle)
        }
    },
    children = children
)

//InventoryType.CHEST -> Bukkit.createInventory(this, width * height, Component.text(title))
//InventoryType.ANVIL -> AnvilGUI.Builder()
//.title(title)
////            .text(text)
//.onComplete { a, b -> get<>() AnvilGUI.Response.close() }
//.open(viewers.first())
//.inventory
*/
